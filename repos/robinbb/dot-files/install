set -eu
cd "$HOME"
alias dot="git --git-dir=/home/anon/repos/robinbb/dot-files/.git --work-tree=${HOME}"
dot config --local status.showUntrackedFiles no
dot restore .
nix profile install nixpkgs#neovim
nix profile install nixpkgs#starship
